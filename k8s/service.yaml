apiVersion: v1
kind: Service
metadata:
  name: sample-app-service
  labels:
    app: sample-app
    environment: dev
    project: csa-automation
spec:
  type: ClusterIP
  selector:
    app: sample-app
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
